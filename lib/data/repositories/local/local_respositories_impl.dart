import 'package:thortbal/domain/repositories/local_repositories.dart';

class LocalRepositoriesImpl extends LocalRepositories {
  @override
  Future<void> saveUserToken(String token) {
    throw UnimplementedError();
  }
}
